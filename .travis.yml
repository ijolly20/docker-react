language: generic

sudo: required
services: 
  - docker
  
before_install:
  - docker build -t ishjolly/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ishjolly/docker-react npm run test